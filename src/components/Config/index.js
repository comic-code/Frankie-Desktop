import { useEffect } from 'react';
import { ConfigContainer } from './styles';

export default function Config() {
  // useEffect(() => {
  //   document.title = 'Frankie - Configurações'
  // }, []);

  return (
    <ConfigContainer>

    </ConfigContainer>
  )
}